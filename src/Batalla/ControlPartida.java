package Batalla;

public class  ControlPartida {

    /*Falso main para la batalla (solo vamos a llamar a las funciones)

    ej / prototipo:

    while(jugador1.heroeSeleccionado.vida > 0 && jugador2.heroeSeleccionado.vida > 0)
    {
       Llamada a la funcion de interfaz grafica que pone la pantalla de combate
       if(jugador1.vida > 0 && jugador2.vida > 0)
       {
        - Llamada a la pantalla de espera para cambio de turno
        - Llamada a la misma funcion pero desde el lado del otro jugador
       }
    }


  pienso que puede llegar a ser asi porque supongo que la que se va a encargar del resto de cosas
  seria la misma clase de interfaz grafica debido a que es la que va a estar a la escucha de
  que acciones realiza cada jugador
     */
}
